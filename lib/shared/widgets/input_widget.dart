import 'package:flutter/material.dart';

class InputWidget extends StatelessWidget {
  final double borderRadius;
  const InputWidget({super.key, this.borderRadius = 10});

  @override
  Widget build(BuildContext context) {
    return TextField();
  }
}
